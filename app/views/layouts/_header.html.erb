<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <%= link_to "Recipes.com", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="#">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Link</a>
        </li>

        <% if current_user %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Friendships
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "My Followers", followers_path, class: 'dropdown-item' %></li>
              <li><%= link_to "I follow", followees_path, class: 'dropdown-item' %></li>
            </ul>
          </li>
        <% end %>
        
      </ul>
      <form class="d-flex" role="search">
        <% if user_signed_in? %>
          <span class="me-2"><%= link_to current_user.email, profile_path, class: "badge bg-secondary p-2", style: "font-size: 12pt; text-decoration: none; color: white" %></span>
          <%= link_to destroy_user_session_path, data: { turbo_method: :delete }, class: "btn btn-primary" do %>
            Logout <i class="bi bi-box-arrow-left"></i>
          <% end %>
        <% else %>
          <%= link_to new_user_session_path, class: "btn btn-primary" do %>
            Login <i class="bi bi-box-arrow-in-right"></i>
          <% end %>
          <%= link_to new_user_registration_path, class: "btn btn-primary ms-2" do %>
            Register <i class="bi bi-box-arrow-in-right"></i>
          <% end %>
        <% end %>
      </form>
    </div>
  </div>
</nav>